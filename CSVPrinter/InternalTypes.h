#pragma once
#include <vector>
#include <string>
#include <filesystem>
#include <memory>

using CSVRow = std::string;
using CSVRows = std::vector<CSVRow>;
using Path = std::filesystem::path;
